info:
  name: Cadastrar aluno
  type: http

http:
  method: POST
  url: http://localhost:3000/alunos
  body:
    type: json
    data: |-
      {
        "id": 5,
        "nome": "Mônica",
        "curso": "ADS"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5