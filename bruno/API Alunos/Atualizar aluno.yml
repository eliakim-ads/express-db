info:
  name: Atualizar aluno
  type: http

http:
  method: PUT
  url: http://localhost:3000/alunos/:id
  body:
    type: json
    data: |-
      {
        "nome": "Vitória",
        "curso": "ADS"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5​